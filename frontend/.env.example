# Frontend — copy to .env: cp .env.example .env
# API base for strict dashboard (no mock). Use proxy to FastAPI :8000 by default.
VITE_API_BASE=
# e.g. VITE_API_BASE=http://localhost:8000 for direct, or leave empty to use Vite proxy /api → :8000
